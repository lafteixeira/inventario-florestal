// Versão exibida na tela (aba Projetos) e usada como nome do cache do
// Service Worker. Suba manualmente a cada mudança que for pra produção —
// é o que a equipe de campo vê pra confirmar que está rodando a versão certa.
export const VERSAO = '1.0.0';
